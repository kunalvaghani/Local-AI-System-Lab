# Initial Risk Register

This register began in Stage 0 and is updated with evidence at each stage. A
planned mitigation is not an implemented control.

| ID | Risk | Likelihood | Impact | Current evidence | Planned response / owning stage |
| --- | --- | --- | --- | --- | --- |
| R-01 | 4 GB VRAM causes model load or context OOM | Medium | High | Stage 9 routes only available models, then retains exact profile admission and budget checks; measured VRAM spans 0 to 1,189 MiB for the pinned model | Retain reserves/OOM handling; require new evidence before activating each added model |
| R-02 | RAM/VRAM estimator diverges from real use | High | High | Constrained VRAM was underpredicted by 160.110 MiB and CPU-safe RAM by 375.172 MiB; reserves covered this sample | Calibrate each model/profile with repeated samples before broad accuracy claims |
| R-03 | Python concurrency blocks cancellation or shutdown | Medium | High | Stage 15 externally creates and cancels an active slow task over HTTP; API task ownership and scheduler/inference cancellation remain cooperative and bounded | Retain cancellation/timeout tests; require process isolation before accepting hostile non-cooperative handlers |
| R-04 | Framework abstractions hide core runtime behavior | Medium | High | Stage 1 core uses stdlib protocols with no framework dependency | Keep core contracts framework-independent; ADR review at each integration |
| R-05 | Unrestricted tools expose host files/processes/secrets | Low | Critical | Stage 14 retains two read-only tools, exact grants, a global `filesystem.read` ceiling, configured path entries/components/suffixes, output checks, and hostile path/escalation tests | Keep risky tools absent; require OS/process isolation and new adversarial evidence before adding side effects |
| R-06 | Nondeterministic model output is misrepresented as replayable | Low | High | Stage 11 classifies model steps nondeterministic, stores input/output hashes, observes them without regeneration, and tests zero additional model calls during replay | Keep the boundary explicit; evaluation may compare quality later but must not relabel generation deterministic |
| R-07 | SQLite contention or crash semantics lose task state | Low | High | Stage 13 retained WAL/FULL, injected a terminal result-write failure, terminated/restarted a worker, and finished with `integrity_check: ok` | Add contention and backup/restore depth; retain ignored database backups where needed |
| R-08 | Benchmarks become cherry-picked or non-reproducible | Medium | High | Stage 16 pins real API limits before execution, compares with the retained Stage 2 median, embeds policy and exact actuals, and keeps the one-run caveat | Expand repeat counts and hardware/model profiles only with declared hypotheses |
| R-09 | Python version ambiguity causes irreproducible installs | Medium | Medium | `requires-python >=3.10` is declared; machine still has 3.10/3.11 entry points | Use `python` consistently; lock dependencies when dependencies are introduced |
| R-10 | llama.cpp/CUDA setup delays real inference | Low | High | Idempotent setup verified pinned prebuilt archives, executable, model, version, and CUDA device; downloads remain large | Retain hashes and rerun setup on a clean machine before portability claims |
| R-11 | Ollama prototype behavior is mistaken for flagship backend behavior | Low | Medium | Stage 2 uses and labels a direct llama.cpp path; Ollama was not used | Keep future adapters and measurements distinct |
| R-12 | Frontend work outruns or fabricates backend capabilities | Low | High | Stage 19 types were derived from real API payloads; the UI preserves null/unavailable evidence, exposes zero real stub calls, and has real-proxy smoke evidence | Require the same payload-first fixtures and retained smoke evidence for every specialist screen |
| R-13 | Scope growth creates interfaces for unimplemented future features | High | Medium | Stage 1 protocols expose only demonstrated lifecycle methods | Continue one-stage gate and require a test/capability for interface growth |
| R-14 | Observability overhead distorts constrained-device performance | Low | Medium | Stage 12 is pull-based with no background sampler; collection time is reported and the controlled durable-only query is measured | Retain bounded windows/limits and benchmark again as database size grows or exporters are added |
| R-15 | Access-restricted hardware APIs reduce profiler fidelity | Medium | Medium | Registry, GlobalMemoryStatusEx, and NVIDIA queries work; physical core count remains unavailable and is explicitly null | Retain sources/confidence and add platform fallbacks only with tests |
| R-16 | Per-request model reload dominates latency | High | Medium | Median model load was 1,128.28 ms and cold TTFT 1,686.85 ms | Compare a persistent server only after profiling and preserve this cold baseline |
| R-17 | Total-device VRAM sampling attributes unrelated GPU use to inference | Medium | Medium | Stage 7 admission and Stage 2 sampling use device-wide `nvidia-smi`; the source/caveat is attached | Add process-aware attribution where supported; keep conservative reserve |
| R-18 | Small-model output completes but violates the specialized role | High | Medium | Stage 4 separates `VALIDATING`/`INVALID_OUTPUT`, but current validation only rejects blank output and cannot detect the Stage 3 semantic drift | Add typed validators/evaluation workloads later; never equate completion with correctness |
| R-19 | Tool capability metadata is mistaken for executable permission | Low | High | Stage 5 converts declarations into explicit grants; resolver separately requires exact tool name and every declared permission | Keep grant and tool schemas inspectable; test denials for every new tool |
| R-20 | Execution state becomes internally inconsistent | Low | High | Stage 10 applies the same legal graph transactionally in SQLite; illegal-transition rollback and restart reconstruction pass | Keep migrations and all state writes behind the state-machine protocol |
| R-21 | A state name implies enforcement that does not exist | Low | High | Stage 6 enforces queue-plus-execution deadlines and maps them to `TIMEOUT`; tests cover queue and active expiry | Keep enforcement tests and distinguish caller-wait timeouts from request deadlines |
| R-22 | Timed-out Python tool handler keeps running | Medium | High | Stage 14 adversarial evidence confirms a cooperative loop observes cancellation after its 20 ms deadline, but Python still cannot kill a hostile thread | Permit vetted cooperative tools only; isolate untrusted/side-effecting handlers in processes before adding them |
| R-23 | Timed-out scheduled operation keeps running outside logical worker accounting | Low | High | Current native inference cooperates with cancellation/termination; Stage 14 also caps the guarded inference path at one slot, but arbitrary hostile operations remain unsupported | Keep scheduler inputs trusted/cooperative; require process isolation before untrusted operations |
| R-24 | Priority scheduling starves background work or uses poor fixed priorities | Medium | Medium | Stage 6 adds aging and maximum-wait promotion; 100/50/10 defaults are baselines, not workload-tuned optima | Measure real queue workloads and revise only from latency/throughput evidence |
| R-25 | Admission recommendation is mistaken for an applied adaptation | Low | High | Stage 9 applies only an available model route and a named profile that independently receives `ACCEPT`; candidates, attempts, and flags are recorded | Keep route/profile tests for every activated model |
| R-26 | A resource profile is presented as universally optimal | High | Medium | Four profiles are labelled hypotheses; one-run results retain caveats and combined-variable confounding | Repeat/isolate experiments and keep workload/hardware-specific evidence |
| R-27 | Zero GPU layers is mistaken for zero VRAM | Low | Medium | Zero layers alone used 311 MiB; explicit `--device none` then measured 0 MiB VRAM | Require/test explicit device isolation for CPU profiles |
| R-28 | Missing or aliased artifacts are presented as available models | Low | High | Registry availability requires both an actual file and configured backend; compact route tests are explicitly controlled and make no inference claim | Keep artifact/backend checks and pin/hash every activated model |
| R-29 | Declared model capabilities or quality ranks are mistaken for output quality evidence | High | Medium | Route metadata is labeled policy input; compact candidate has no benchmark and semantic validation remains absent | Add model-specific evals before capability/quality tuning claims |
| R-30 | A task memory budget is assumed to prevent every peak | Medium | High | Stage 9 checks profile estimates before scheduling and reports sampled peaks afterward; estimator misses remain documented | Calibrate per model/profile and distinguish predicted enforcement from observed usage |
| R-31 | Recovery duplicates model or tool side effects | Low | Critical | Automatic retry is limited to explicit pre-invocation `recovery_ready`; terminal and in-flight tasks are rejected by tests | Add idempotency keys before expanding recovery boundaries |
| R-32 | Terminal state is committed but output is absent after a narrow crash window | High | High | Stage 16 reproduced it again: 9/9 expected chaos outcomes but only 8/9 contained; persistence/recovery and fault-injection maturity remain `PARTIAL` | Add a combined terminal-result transaction or explicit repair workflow; frontend must represent missing output honestly |
| R-33 | SQLite schema drift silently corrupts reconstruction | Low | High | Schema v2 is tracked, v1-to-v2 migration is idempotent, newer versions are rejected, and typed reconstruction is tested | Require forward-only migrations and backup/restore tests for future versions |
| R-34 | Trace payloads retain sensitive prompts, tool arguments, or outputs | Medium | High | Stage 15 API omits trace input/output payloads, run metadata, system prompts, and absolute model paths; validated objectives/outputs still remain in ignored local SQLite and task responses | Define encryption-at-rest, retention, deletion, task-response policy, and export redaction before multi-user or remote use |
| R-35 | Hash-valid replay is mistaken for full behavioral re-execution | Medium | High | Replay verifies canonical evidence and deterministic state reducers; it explicitly observes model/environment steps and skips tool side effects | Preserve outcome labels and never claim native token-level or external side-effect replay |
| R-36 | A live scheduler snapshot is mistaken for historical queue state | Medium | Medium | Stage 12 labels `scheduler.snapshot()` separately from durable scheduler-event distributions; an existing-database report opens a fresh reporting runtime | Preserve source labels; a future service must define instance identity and scrape semantics |
| R-37 | Observability queries slow or contend with a growing runtime database | Medium | Medium | Stage 12 uses one read transaction, bounded recent drill-down, WAL, and configurable limits; per-task drill-down currently uses bounded follow-up queries | Add indexes/query plans, pagination, and batched joins from measured database growth before external API exposure |
| R-38 | Fault injection activates during normal runtime use | Low | Critical | API requires literal confirmation, caps unique selected scenarios at three, and runs a separate stub runtime/unique database; the serving runtime remains running and unarmed in tests | Keep isolation/confirmation mandatory; add authorization before any non-loopback deployment |
| R-39 | Simulated faults are presented as proof of physical fault tolerance | Medium | High | Eight scenarios are protocol-level simulations; only the agent-crash scenario kills a process, and the report labels quality impact unavailable | Preserve labels and add real resource/process/database experiments only with bounded safety controls |
| R-40 | Prompt injection changes model behavior or induces unsafe actions | High | Critical | Stage 14 JSON-encodes untrusted objectives and keeps tool/path/network authority in deterministic code; the structural stub case passed, but no delimiter or system prompt guarantees model compliance | Preserve deterministic least privilege, test real models and indirect content, and require approval for any future high-impact action |
| R-41 | Application-level network denial is mistaken for OS isolation | Medium | Critical | Stage 14 registers no network tool and denies the network permission category, but does not install firewall rules or isolate the pinned model process | Label the boundary; require a container/VM/firewall design before executing untrusted binaries or network-capable tools |
| R-42 | Pattern-based secret detection misses a secret or blocks benign text | Medium | High | Stage 14 tests representative token/bearer/private-key patterns and redaction, but the detector is intentionally finite | Add secret-source integration and measured detector tests; never treat pattern scans as complete DLP |
| R-43 | A passing adversarial suite is presented as a security certification | High | High | The CLI, retained report, ADR, and stage report explicitly state that 14/14 PASS covers only bounded local cases | Keep scenario scope and limitations beside every result; require independent review and broader threat modeling before deployment claims |
| R-44 | Development HTTP server is exposed as a production service | Medium | Critical | Stage 15 validates a literal loopback bind, labels the scope in discovery/docs, serves no files, and records Python's production warning | Keep non-loopback binding rejected; select and harden a production server only if a later approved deployment requires it |
| R-45 | Slow or malicious clients exhaust handler threads or SSE connections | Medium | High | Request bodies, task capacity, task deadlines, SSE polling, and stream duration are bounded; handler connection count is not globally capped | Add authenticated rate/connection limits and production-server controls before remote/multi-user operation |
| R-46 | API process restart loses accepted-task ownership or SSE cursor state | Medium | High | Stage 16's complete suite verifies completed-task fallback after API restart; active records/cursors remain process-local and safe recovery remains limited to explicit pre-invocation checkpoints | Add durable API idempotency/ownership only with a defined active-task recovery contract |
| R-47 | A green test count is mistaken for complete backend maturity | Medium | High | Stage 16 separately reports 14 binary requirement checks and nine subsystem maturity classifications; overall remains `PARTIAL` despite release-candidate status | Preserve the two-axis gate and require a new full run for threshold/classification changes |
| R-48 | Acceptance thresholds are loosened after observing a regression | Medium | High | Thresholds are tracked in `configs/acceptance.json`, embedded in the retained result, and compared against the retained Stage 2 baseline | Review threshold diffs as release decisions and never edit retained results |
| R-49 | Visual novelty obscures runtime truth | Low | High | Stage 19 runtime pulse/task rail map directly to documented fields and endpoints; missing inference values render unavailable while measured zero remains zero | Preserve unit/source/window and text-equivalent rules as richer visuals arrive |
| R-50 | Large traces/events lock the browser main thread | Medium | High | Stage 21 verifies a 10,000-step fixture while rendering at most 100 timeline rows, defers search, and memoizes derived maps; the API still transfers the complete selected trace | Add API pagination/streaming and list virtualization if measured production traces outgrow the current bounded local contract |
| R-51 | Live SSE or trace updates steal focus, overwhelm assistive technology, or cause rerender storms | Medium | High | SSE remains capped at 200/30; trace polling runs only for the active selected task, does not use a flooding live region, and retains expansion by stable URL step ID | Add a user-controlled pause only if measured long-running traces make one-second refresh disruptive |
| R-52 | Graphs and timelines become inaccessible alternate truths | Medium | High | Stage 21 uses a semantic ordered list, real buttons, text labels for every node/gap/outcome, keyboard expansion, and a dedicated axe scan; CSS bars are supplementary | Complete manual screen-reader, zoom, forced-color, computed-contrast, and reduced-motion validation before a conformance claim |
| R-53 | Frontend dependencies create a generic appearance or excessive bundle | Low | High | Stage 21 adds no graph, chart, router, editor, terminal, motion, or persistence dependency and remains under the 250 KiB gzip gate | Record each later bundle delta and reject overlapping or unused dependencies |
| R-54 | UI hides `PARTIAL`, `DEFERRED`, unavailable, stale, or missing-output backend states | Medium | High | Stage 19 maps real connection/task failures, request IDs, unavailable models, null hardware/inference samples, and absent output without optimistic coercion | Add explicit completed-with-missing-output fixture coverage when the Stage 20 task surface expands |
| R-55 | Automated accessibility output is presented as WCAG conformance | High | High | Stage 18 axe scan reports zero violations but explicitly disables jsdom color contrast and labels the result non-certifying | Retain manual keyboard, forced-color, zoom/reflow, computed contrast, reduced-motion, NVDA Chrome/Firefox acceptance in Stage 25 |
| R-56 | Shallow owned routing becomes brittle as task/trace detail navigation grows | Medium | Medium | Stage 21 adds one independently validated optional `?step=` selector, preserves task/query state, and tests deep expansion plus back/forward-compatible history; hierarchy remains shallow | Adopt a maintained router if Stage 22+ introduces nested resources or more interdependent parameter shapes |
| R-57 | A local development shell is mistaken for a remotely hardened frontend | Medium | Critical | Vite and API both bind to literal loopback; docs label the shell local-only and no deployment configuration is present | Keep non-loopback exposure absent; any hosted/multi-user proposal requires authentication, transport, CSP, deployment, and threat-model work |
| R-58 | Frontend polling amplifies load or hides a disconnected API behind stale data | Medium | Medium | Query cadences are explicit (1–60 seconds), requests use abort signals, retry once, and the connection indicator is driven by health rather than cached task success | Measure API contention with Stage 20 graph traffic and add visible stale-age/backoff semantics if needed |
| R-59 | Refreshing a terminal task loses its transient SSE rail | High | Medium | The authoritative terminal record remains URL-inspectable and contains durable state history, but Stage 19 intentionally does not reopen a completed stream | Stage 20 task graph should project durable state history for refresh/restart inspection without pretending it is live SSE |
| R-60 | A single selected task is mistaken for a complete task-history view | Medium | Medium | The UI labels selected execution and documents that the API has no list-tasks endpoint; it does not synthesize history from metrics | Add a bounded paginated backend contract before implementing a task explorer |
| R-61 | Aborted browser polls create noisy Windows development-server tracebacks | High | Low | Stage 19 shutdown reproduced `ConnectionAbortedError` when HMR/teardown cancelled concurrent hardware/metrics requests; completed responses, 150 tests, and clean shutdown still passed | Treat `ConnectionAbortedError` as a normal disconnected-client boundary in a future API hardening change and add a real-socket regression test |
| R-62 | Scheduler snapshot eviction is mistaken for missing execution | Medium | Medium | Stage 20 uses the live request when present and the selected terminal task's authoritative retained scheduler metadata after eviction; the global ledger still shows only currently reported rows | Add a bounded paginated scheduler-history API only if global history becomes approved scope |
| R-63 | Projected queue order is mistaken for a preemption guarantee | Medium | Medium | Stage 20 sorts only queued rows using reported policy/effective priority/stable sequence and explicitly states running work is non-preemptible | Keep policy labels beside every queue projection and test future scheduler semantics before UI changes |
| R-64 | Absent stub admission is visually converted into acceptance | Low | High | Stage 20 renders `Not reported`; the live smoke retains `admission: null` and does not infer a decision | Require real decision payloads for estimate/constraint rendering and keep null fixtures |
| R-65 | Hash-only safe trace evidence is mistaken for raw prompt/output visibility | Medium | High | Stage 21 labels the API payload policy beside expanded hashes and never reconstructs or invents redacted content | Keep raw payload access outside the browser contract unless a separately secured, explicitly approved design exists |
| R-66 | Time between recorded events is mistaken for component execution latency | Medium | High | Stage 21 labels every bar as a timestamp gap (`Δ`) and shows exact milliseconds rather than claiming per-step duration | Add true start/finish duration fields at the backend before presenting execution latency bars |
| R-67 | Replay is mistaken for re-running model generation or tool side effects | Medium | Critical | Replay is an explicit button; the UI and ADR state deterministic reducers are checked, nondeterministic work is observed, and side effects are skipped | Preserve per-step outcome/reason labels and never auto-trigger replay on load or refresh |
| R-68 | A same-run replay result is presented as cross-run divergence comparison | Medium | High | The Trace Explorer explicitly reports that no cross-run comparison API exists and displays only source-run replay outcomes | Add a bounded, redacted backend comparison contract before enabling run-to-run comparison controls |

## Review rule

At each stage boundary, update evidence, add newly discovered risks, and change
likelihood/impact only when there is a stated reason. Closed risks remain in the
record with the verification that justified closure.
